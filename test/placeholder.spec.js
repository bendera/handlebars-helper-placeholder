'use strict';

const assert = require('assert');
const hbs = require('handlebars');
const placeholder = require('..');

describe('placeholder helper', () => {
  describe('without parameters', () => {
    it('should return the default svg image', () => {
      const expected = `
      
      `

      assert.equal(1, 2);
    });
  })
});
